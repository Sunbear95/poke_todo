{
  "name": "poke-todo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "expo/AppEntry.js",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "start": "node scripts/serve.mjs",
    "mobile": "npx expo start --lan",
    "mobile:tunnel": "npx expo start --tunnel",
    "mobile:web": "npx expo start --web",
    "test": "node --test",
    "build": "node scripts/build-check.mjs",
    "smoke": "node test/smoke.mjs"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "playwright": "^1.60.0"
  },
  "dependencies": {
    "@expo/vector-icons": "^15.1.1",
    "expo": "^56.0.8",
    "react": "^19.2.3",
    "react-native": "^0.85.3"
  }
}
